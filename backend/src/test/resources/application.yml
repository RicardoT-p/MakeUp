spring:
  datasource:
    url: jdbc:h2:mem:makeup;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  sql:
    init:
      mode: never
  cache:
    type: none
mybatis:
  configuration:
    map-underscore-to-camel-case: true

